[pytest]
markers =
    timeout(seconds): mark tests with a timeout value (used by ESP32 hardware bridge tests)
    visual: marks tests as requiring graphical display (deselect with '-m "not visual"')

filterwarnings =
    # Silence pytest experimental API warnings (e.g., record_xml_attribute)
    ignore::pytest.PytestExperimentalApiWarning
    # Ignore collection warnings for classes with __init__ (helper classes)
    ignore::pytest.PytestCollectionWarning

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Timeout defaults for different test types
timeout = 300
timeout_method = thread
